const access_control_origin = "Access-Control-Allow-Origin";
const access_control_methods = "Access-Control-Allow-Methods";
const access_control_headers = "Access-Control-Allow-Headers";
const server_start_text = "Server starts at:";
const sigint_code = "SIGINT";
const middleware_empty_path = "/";

module.exports = {
    access_control_origin,
    access_control_methods,
    access_control_headers,
    server_start_text,
    sigint_code,
    middleware_empty_path
}